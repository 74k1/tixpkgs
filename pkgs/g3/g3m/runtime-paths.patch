diff --git a/src/adapters/g3mtool_adapter.py b/src/adapters/g3mtool_adapter.py
index f3ac67c..3553f47 100644
--- a/src/adapters/g3mtool_adapter.py
+++ b/src/adapters/g3mtool_adapter.py
@@ -5,6 +5,7 @@ import logging
 import os
 import platform
 import re
+import shutil
 import subprocess
 import threading
 from collections.abc import Callable
@@ -54,7 +55,7 @@ class G3MToolManager:
 
     def _get_configured_xdelta_path(self) -> str | None:
         path = str(self._get_local_config().get("custom_xdelta_path", "") or "").strip()
-        return path or None
+        return path or shutil.which("xdelta3") or shutil.which("xdelta")
 
     def _find_executable(self) -> str | None:
         custom_path = self._get_configured_g3mtool_path()
@@ -70,7 +71,7 @@ class G3MToolManager:
         exe_name = "G3MTool.exe" if self.platform == "windows" else "G3MTool"
         exe_path = os.path.join(base_path, exe_name)
         if os.path.exists(exe_path):
-            if self.platform != "windows":
+            if self.platform != "windows" and not os.access(exe_path, os.X_OK):
                 try:
                     os.chmod(exe_path, 0o700)
                 except Exception as e:
@@ -82,6 +83,13 @@ class G3MToolManager:
                 self._logged_executable_paths.add(exe_path)
             self._cached_executable_paths[self.platform] = exe_path
             return exe_path
+        system_executable = shutil.which(exe_name)
+        if system_executable:
+            if system_executable not in self._logged_executable_paths:
+                logger.info(f"Found G3MTool: {system_executable}")
+                self._logged_executable_paths.add(system_executable)
+            self._cached_executable_paths[self.platform] = system_executable
+            return system_executable
         logger.warning(f"G3MTool not found at {exe_path}")
         self._cached_executable_paths[self.platform] = None
         return None
diff --git a/src/app/startup.py b/src/app/startup.py
index 02ffab9..5de9774 100644
--- a/src/app/startup.py
+++ b/src/app/startup.py
@@ -217,10 +217,14 @@ def register_url_protocol():
     if getattr(sys, "frozen", False):
         executable_path = f'"{sys.executable}"'
     else:
-        main_script_path = os.path.abspath(
-            os.path.join(get_launcher_dir(), "..", "src", "main.py")
-        )
-        executable_path = f'"{sys.executable}" "{main_script_path}"'
+        launcher_path = shutil.which("g3m")
+        if launcher_path:
+            executable_path = f'"{launcher_path}"'
+        else:
+            main_script_path = os.path.abspath(
+                os.path.join(get_launcher_dir(), "..", "src", "main.py")
+            )
+            executable_path = f'"{sys.executable}" "{main_script_path}"'
     system = platform.system()
     try:
         if system == "Windows":
@@ -250,15 +254,14 @@ def register_url_protocol():
             with open(desktop_file_path, "w", encoding="utf-8") as f:
                 f.write(desktop_file_content)
             if shutil.which("xdg-mime"):
-                for mime_type in URL_PROTOCOL_MIME_TYPES:
-                    QProcess.startDetached(
-                        "xdg-mime",
-                        [
-                            "default",
-                            URL_PROTOCOL_DESKTOP_FILENAME,
-                            mime_type,
-                        ],
-                    )
+                QProcess.startDetached(
+                    "xdg-mime",
+                    [
+                        "default",
+                        URL_PROTOCOL_DESKTOP_FILENAME,
+                        *URL_PROTOCOL_MIME_TYPES,
+                    ],
+                )
     except Exception as e:
         logger.warning(f"Failed to register URL protocol handler: {e}", exc_info=True)
 
diff --git a/src/ui/dialogs/downloads_dialog.py b/src/ui/dialogs/downloads_dialog.py
index 8891f49..da0d943 100644
--- a/src/ui/dialogs/downloads_dialog.py
+++ b/src/ui/dialogs/downloads_dialog.py
@@ -1,5 +1,6 @@
 """Non-modal Downloads dialog showing download history with per-item actions."""
 
+import contextlib
 import logging
 import os
 
@@ -286,9 +287,14 @@ class DownloadsDialog(QDialog):
         self._manager.record_removed.connect(self._on_record_removed)
 
     def closeEvent(self, event):
-        self._manager.record_added.disconnect(self._on_record_added)
-        self._manager.record_updated.disconnect(self._on_record_updated)
-        self._manager.record_removed.disconnect(self._on_record_removed)
+        signal_slots = (
+            (self._manager.record_added, self._on_record_added),
+            (self._manager.record_updated, self._on_record_updated),
+            (self._manager.record_removed, self._on_record_removed),
+        )
+        for signal, slot in signal_slots:
+            with contextlib.suppress(TypeError, RuntimeError):
+                signal.disconnect(slot)
         super().closeEvent(event)
 
     def _populate(self):
